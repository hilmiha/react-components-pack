import './styles.css'

const Spinner = () =>{
    return (
        <div 
            className="spinner"
            style={{
                borderTopColor:"var(--brand700)"
            }}
        />
    )
}

export default Spinner